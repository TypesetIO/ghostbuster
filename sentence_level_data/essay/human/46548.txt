It is pre-formed by carefully choosing values for the model input parameters (within their respective uncertainty ranges) by comparing the model predictions for a given set of assumed conditions with observed data for the same conditions.