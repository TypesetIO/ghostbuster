In particular, given limited consistency as to how missed appointments (ie, definition and source of data) are operationalized in DM care, 21 24 we attempted to extract the definition of missed appointments and source of data used in each study.